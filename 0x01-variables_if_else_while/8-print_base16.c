#include <stdio.h>
/**
 * main - Beiginning of code
 * Return: 0 (good looks)
 */
int main(void)
{
	char q;

	int s;

	q = 'a';
	s = '0';
	while
		(q < 10) {
			putchar(q + '0');
			q++;

		}
	while
		(s <= 'f') {
			putchar(s);
			s++;
		}
	putchar('\n');
	return (0);
}

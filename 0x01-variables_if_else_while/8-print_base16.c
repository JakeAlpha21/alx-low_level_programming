#include <stdio.h>
/**
 * main - Beiginning of code
 * Return: 0 (good looks)
 */
int main(void)
{
	char q;

	int s;

	q = 'a';
	s = 0;
	while
		(s < 10) {
			putchar(s + '0');
			s++;

		}
	while
		(q <= 'f') {
			putchar(q);
			q++;
		}
	putchar('\n');
	return (0);
}

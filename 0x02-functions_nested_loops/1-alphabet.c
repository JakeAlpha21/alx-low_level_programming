#include "main.h"

/**
 * print_alphabet - Make the alphabet lowercase
 * Return: void (C'est Du Bon)
 */

void print_alphabet(void)
{
char ch;
for (ch = 'a'; ch <= 'z'; ch++)
_putchar(ch);
_putchar('\n');
}
